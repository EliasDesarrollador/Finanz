# MySQL connection
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=expense_tracker

# API port
PORT=3000

# Public server base URL used for OAuth redirects
SERVER_BASE_URL=http://localhost:3000
# Frontend URL to return after OAuth if not provided dynamically
OAUTH_REDIRECT_DEFAULT=http://localhost:5173/auth

# Google OAuth credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Facebook OAuth credentials
FACEBOOK_CLIENT_ID=
FACEBOOK_CLIENT_SECRET=
